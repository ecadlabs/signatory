server:
  # Address for the main HTTP server to listen on
  address: :6732
  # Address for the utility HTTP server to listen on
  utility_address: :9583

vaults:
  # Name is used to identify backend during import process
  kms:
    driver: cloudkms
    config:
      project: signatory-testing
      location: europe-north1
      key_ring: hsm-ring
  azure:
    driver: azure
    config:
      vault: https://signatory.vault.azure.net/
      tenant_id: 50c46f11-1d0a-4c56-b468-1bcb03a8f69e
      client_id: b7ddefbb-fcac-4068-813b-b3450c36b9a7
      client_pkcs12_certificate: /Users/asphyx/Projects/signatory/service-principal.pfx

# List enabled hashes here
tezos:
  # Default policy
  tz3VfoCwiQyMNYnaseFLFAjN9AQJQnhvddjG:
  # Explicit policy
  tz3aTwpna6m9qsw4YZVFad1nsm5cGgWHVQ8R:
    allowed_operations:
      # List of [generic, block, endorsement]
      - generic
      - block
      - endorsement
    allowed_kinds:
      # List of [endorsement, ballot, reveal, transaction, origination, delegation, seed_nonce_revelation, activate_account]
      - transaction
      - endorsement
